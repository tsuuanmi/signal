# Signal Software Requirements Specification

## 1. Purpose

Signal is a deterministic Rust CLI for reference-free base re-calling and research analysis of one Sanger ABIF/AB1 chromatogram against one short reference. Normative terms **MUST**, **SHOULD**, and **MAY** apply to every `SRS-*` item.

## 2. Inputs and process boundary

- **SRS-IN-001:** `analyze` and `basecall` MUST accept exactly one regular non-empty AB1 path. `sample` MUST accept one sample identifier and one or more regular non-empty AB1 paths. Reference-guided commands MUST require one regular non-empty FASTA path; `basecall` MUST NOT accept a reference. Directories, manifest/list compatibility inputs, and globs MUST NOT be accepted by the core CLI.
- **SRS-IN-002:** AB1 bytes MUST begin with `ABIF`; every directory count, size, product, offset, inline payload, and allocation MUST be checked before use.
- **SRS-IN-003:** Canonical decode MUST require `DATA.9-12`, `FWO_.1`, and `PLOC.2`; FWO MUST be an exact A/C/G/T permutation; channels MUST be equally sized; PLOC MUST be strictly increasing and in range.
- **SRS-IN-004:** `PLOC.2` is required; `PBAS.2` and `PCON.2` MAY be consumed as optional vendor evidence. `P2BA.1` MUST be ignored. Uppercase IUPAC vendor bases and the ABIF one-byte byte/char PCON representations MUST be accepted. No alternate-tag fallback is permitted.
- **SRS-IN-008:** Optional PBAS/PCON cardinality MUST NOT define or extend the scientific locus series. A present vendor series whose decoded length differs from the valid PLOC count MUST remain non-fatal trace-integrity evidence; Signal MUST process exactly the PLOC-defined loci and boundedly ignore unavailable/excess vendor positions.
- **SRS-IN-005:** A reference-guided `analyze` or `sample` FASTA MUST contain exactly one non-empty A/C/G/T/N record no longer than 50,000 bases. `basecall` MUST perform no FASTA I/O.
- **SRS-IN-006:** Empty, missing, malformed, unsupported, over-limit, or unreadable input MUST return a typed error without panic or result file.
- **SRS-IN-007:** Generic ABIF directory parsing MUST follow the container specification rather than imposing scientific-tag assumptions globally. A directory item MUST contain at least its logical `element_size × element_count` payload; a larger `data_size` that is permitted by ABIF padding/reserved-space behavior MUST NOT by itself make the file invalid. Decoders for required scientific tags MAY impose stricter tag-specific type, element-size, cardinality, and semantic constraints.
- **SRS-IN-010:** The single-read reference-guided command MUST be `signal analyze <trace.ab1> --reference <reference.fasta>` and derive `results/<trace-stem>.json`. The reference-free command MUST be `signal basecall <trace.ab1>` and derive `results/<trace-stem>.basecalls.json`. The sample command MUST be `signal sample <sample-id> <trace.ab1>... --reference <reference.fasta>` and derive `results/<sample-id>.sample.json`. No output-path or format compatibility option is permitted.
- **SRS-IN-011:** Help/version MUST succeed without reading analysis inputs.
- **SRS-IN-012:** Invalid CLI arguments MUST produce concise stderr and nonzero status.

## 3. Configuration

- **SRS-CFG-001:** Signal MUST load `SIGNAL_CONFIG` or `config/signal.toml`; it MUST NOT parse `.env`.
- **SRS-CFG-002:** Schema version MUST be integer `5`; all documented sections/keys are required.
- **SRS-CFG-003:** Unknown/duplicate keys, missing fields, non-finite values, unsupported enums/versions, and invalid ranges MUST fail.
- **SRS-CFG-004:** Environment MUST NOT override individual scientific settings. `SIGNAL_LOG_DIR` MAY select only the operational log directory.
- **SRS-CFG-005:** JSON MUST include the configuration checksum but MUST omit method constants, the local config path, and the effective-value expansion. Effective values and configuration schema version 5 remain in the selected strict TOML.
- **SRS-CFG-006:** Hard caps MUST bound AB1 bytes, reference length, alignment cells, indel length, and reachable peak thresholds before unsafe allocation.
- **SRS-CFG-007:** Configuration MUST require a positive `sample_reconciliation.minimum_comparable_bases` and finite `sample_reconciliation.minimum_overlap_agreement` in `(0,1]`; these settings apply only after independently placed reads enter sample reconciliation.

## 4. Signal-derived base re-calling

- **SRS-BC-001:** Calls MUST derive from analyzed A/C/G/T signals at validated PLOC loci. PBAS MUST NOT become final algorithm output.
- **SRS-BC-002:** Windows MUST use neighboring PLOC midpoints with bounded first/last extrapolation.
- **SRS-BC-003:** Each channel MUST use its strongest positive local maximum under the documented plateau rule, or its PLOC sample with an explicit fallback marker.
- **SRS-BC-004:** No positive strongest signal or exact strongest tie MUST yield primary N.
- **SRS-BC-005:** For a uniquely strongest positive selected peak, a channel MUST qualify only when both its positive selected peak divided by the primary peak height and its positive signal at the primary peak sample divided by the primary peak height are greater than or equal to `secondary_peak_ratio`. One qualifying channel MUST call its base canonically; two qualifying channels MUST use the standard two-base IUPAC code on the strongest primary; three qualifying channels MUST retain the strongest primary with unresolved `N` ambiguity; four qualifying channels MUST be unresolved `N` for both primary and ambiguity.
- **SRS-BC-006:** Every call MUST internally retain its original index, PLOC, call-window sample bounds, four peak heights/positions/sources, primary, ambiguity, qualifying channels, and vendor agreement. Compact analysis JSON MUST expose each variant-associated call as reference-oriented base plus co-located A/C/G/T primary-event channel heights and uncalibrated quality; selected peak positions/sources and ABIF implementation coordinates remain internal.
- **SRS-BC-007:** No sample-specific poly-C correction or reference-aware basecall rescue is permitted; configured biological regions apply only after variant normalization.
- **SRS-BC-008:** The MVP is a signal-derived **re-caller at vendor-defined loci**, not an independent de novo locus detector. `PLOC.2` defines the call loci for the current method; `PBAS.2` and `PCON.2` remain optional vendor evidence and MUST NOT determine Signal's final base identity.

## 5. Observational signal processing

- **SRS-SIG-001:** Signal processing MUST read the immutable analyzed A/C/G/T channels and basecalling evidence; it MUST NOT replace decoded channels or selected peaks.
- **SRS-SIG-002:** Configuration MUST require a rolling window in `5..=10` bases, a finite positive minimum primary SNR, and a minimum noisy-window run length of at least 2.
- **SRS-SIG-003:** Every internally calculated window MUST have the complete configured width and stride one with explicit 0-based half-open call and sample intervals. Compact JSON MUST emit merged noisy regions only, not individual windows.
- **SRS-SIG-004:** Baseline, first-difference MAD noise, primary SNR, secondary SNR, finite noise flooring, rounding, and threshold comparison MUST follow the documented `signal.windowed_snr/v1` formula.
- **SRS-SIG-005:** Overlapping or adjacent candidate-noisy windows MUST be unioned without bridging clean gaps, and a noisy interval MUST require at least the configured minimum run length.
- **SRS-SIG-006:** Candidate-noisy window/region annotations MUST NOT alter quality scores, trim bounds, alignment, warning totals, or variant eligibility. `LocusEvidence` / `EvidenceProfile` MAY be consumed by reference alignment only under the explicit alignment requirements below. No signal feature may be described as Phred, error probability, genotype, or heteroplasmy evidence.
- **SRS-SIG-007:** A read shorter than the configured signal window MUST fail with a typed signal-processing error rather than emit partial windows.
- **SRS-SIG-008:** Signal processing MUST derive one internal `LocusEvidence` record per validated PLOC locus directly from analyzed A/C/G/T channels and shared PLOC-window geometry. Event refinement MUST consider positive local maxima of total non-negative baseline-corrected A/C/G/T amplitude inside the locus window and select the candidate nearest PLOC; equally distant candidates MUST prefer greater total corrected signal and then the lower sample coordinate. If no positive total-signal local maximum exists, the validated PLOC sample MUST be used. Event selection MUST remain independent of primary/ambiguity calls, selected channel identity, qualifying-channel membership, alignment, and reference sequence.
- **SRS-SIG-009:** `EvidenceProfile` MUST normalize positive corrected A/C/G/T amplitudes at the refined event sample and MUST NOT depend on primary/ambiguity calls, selected basecall peaks, qualifying-channel membership, or `secondary_peak_ratio`. A locus with zero total corrected signal MUST have no profile; no uniform or reference-guided fallback is permitted.
- **SRS-SIG-010:** Signal processing MUST derive one immutable trace-integrity record containing PLOC count; optional PBAS/PCON counts; minimum/median/maximum adjacent PLOC spacing when at least two loci exist; the count of analyzed channel samples exactly equal to signed-16-bit extrema; and, when positive locus event signal exists, the six-decimal ratio of maximum to median total corrected event signal.
- **SRS-SIG-011:** Trace-integrity observations MUST NOT rewrite decoded channels, add/remove PLOC loci, change base calls, alter quality/trim, steer reference placement, or change variant eligibility. Vendor-length mismatch and exact clipping MAY contribute warning counts; the event-signal ratio MUST remain unthresholded observation evidence until a separately specified artifact classifier exists.

## 6. Quality control

- **SRS-QC-001:** Each call MUST receive a documented ambiguity/spacing penalty and bounded relative score.
- **SRS-QC-002:** The relative score MUST be explicitly uncalibrated. PCON MUST remain separate internally and apply only when PBAS agrees with the re-called primary; compact JSON MUST omit both the calibration flag and vendor evidence.
- **SRS-QC-003:** A zero maximum penalty MUST produce maximum relative scores without division by zero.
- **SRS-QC-004:** Trimming MUST remove only left/right tails and internally retain the best section, half-open trim interval, retained sequence, penalty, score, and vendor quality applicability. Compact JSON MUST expose only the global trim interval and each variant-associated call's uncalibrated relative score under the public field `quality`; sequences, penalties, calibration flags, and vendor quality MUST be omitted.
- **SRS-QC-005:** Fewer than configured minimum retained bases MUST fail with a typed QC error.

## 7. Alignment

- **SRS-ALN-001:** Signal MUST align retained forward and reverse-complement evidence-profile queries with affine-gap semi-global Gotoh, consuming the complete retained query and allowing free reference flanks.
- **SRS-ALN-002:** Canonical reference substitution scoring MUST use the retained `EvidenceProfile` mass for that reference base as a fixed-point expected match/mismatch score. A missing profile or non-canonical reference base MUST use the configured ambiguous score. A gap of length `k` MUST retain `open + k × extension` semantics in the same fixed-point score scale.
- **SRS-ALN-003:** Traceback MUST use the documented deterministic state and gap-extension tie order. On equal scores the state preference is Match > Deletion > Insertion.
- **SRS-ALN-004:** Circular references MUST align against a doubled sequence, consume at most one reference length, canonicalize modulo coordinates, and expose two segments when crossing origin.
- **SRS-ALN-005:** Both orientations MUST be evaluated internally. JSON MUST report only the selected orientation, callable-base count, callable identity, unresolved-base count, gap-open count, reference segments, and origin-wrap flag. Score, gapped rows, operation runs, traceback columns, and redundant match/mismatch counts MUST be omitted; original-call mapping is exposed through each variant's direct `calls` array.
- **SRS-ALN-006:** An exact best profile-score orientation tie, a genuinely distinct equally scoring placement after canonicalization, insufficient callable columns, low identity, over-limit matrix, or invalid traceback MUST fail explicitly. Repeat-equivalent optimal indel placements MUST be canonicalized under `SRS-ALN-012` rather than treated as distinct placement failures. Primary-sequence exact-match/mismatch counts MUST NOT break a profile-score orientation tie.
- **SRS-ALN-007:** The selected alignment MUST determine the read's covered reference region from traceback. The default short-reference analysis MUST NOT require or consume an HV/amplicon/expected-region classifier to constrain placement.
- **SRS-ALN-008:** Filename, amplicon/HV label, primer label, declared forward/reverse direction, or other assay metadata MUST NOT change scientific placement for identical AB1, reference, configuration, and method inputs. Such metadata MAY be introduced only as provenance or post-mapping QC unless a separately versioned method explicitly defines otherwise.
- **SRS-ALN-009:** The complete one-read scientific products after variant extraction MUST be representable as one `ReadObservation` boundary containing input identity, calls, signal observations, quality control, selected alignment, and read-level variants.
- **SRS-ALN-010:** Alignment score arithmetic MUST use a deterministic fixed-point scale of 1024 integer units. Profile support MUST be quantized by `round(weight × 1024)` before DP comparison, and match/mismatch/ambiguous/gap score deltas MUST all use the same scale.
- **SRS-ALN-011:** `callable_columns`, `callable_identity`, unresolved-query count, exact matches, and mismatches MUST remain derived from the retained primary sequence on the selected traceback. Existing callable/identity admission thresholds therefore remain primary-sequence gates after profile-aware placement until separately versioned semantics are adopted.
- **SRS-ALN-012:** Among maximum-score tracebacks that differ only by sequence-equivalent shifting of the same indel through a homopolymer or tandem repeat, Signal MUST emit one canonical reference-oriented alignment. Equivalent gap content SHOULD use the fewest contiguous gap runs, then each gap MUST be placed furthest 3' with respect to the rCRS light strand (right-most in ordinary increasing rCRS coordinates). Canonicalization MUST NOT choose a lower-scoring path, collapse non-equivalent edit explanations, or shift an indel across the rCRS origin seam. Variant representation MUST NOT silently undo this canonical alignment without an explicitly separate representation contract.

## 7.1 Sample evidence and future interpretation contract

- **SRS-SAMPLE-001:** Multi-read sample analysis MUST process every trace independently through the same authoritative read-observation path used by single-read analysis before cross-read reconciliation.
- **SRS-SAMPLE-002:** Cross-read overlap and support MUST be discovered from mapped reference coordinates/variants rather than inferred from canonical F/R pair names.
- **SRS-SAMPLE-003:** A usable read MUST NOT be rejected solely because a nominal F/R partner is missing.
- **SRS-SAMPLE-004:** Sample evidence MUST contain one deterministic read registry sorted by input SHA-256. Reviewer-facing filename-stem provenance, stable SHA-256 identity, trace-integrity evidence, derived orientation, and mapped post-trim coverage MUST be defined once per read; locus and variant evidence MUST reference that registry by unique reviewer-facing read name. Filename, amplicon, primer, or replicate grouping MUST NOT be treated as a scientific placement or exclusive merge key. Duplicate trace content MUST NOT contribute twice.
- **SRS-SAMPLE-005:** Sample evidence MUST serialize locus observations only at positions where at least one covering read is alternate, unresolved, or deleted. Every covering read at such a differential locus MUST remain explicit, including canonical reference support with its observed base and quality. For a read, a position inside its mapped reference segments but absent from `locus_differences[]` is a canonical reference match; a position outside its mapped segments is uncovered. Missing coverage MUST NOT become reference support.
- **SRS-SAMPLE-006:** Normalized variant support MUST retain the contributing read name, configured eligibility, exact exclusion reasons, and reviewer-facing reference-oriented base/peak/quality evidence; read-level filtering MUST NOT erase an observed normalized variant. Future sample variants and discordance states MUST derive from this evidence; a consensus sequence MAY be emitted only as a downstream projection.
- **SRS-SAMPLE-007:** After deterministic SHA-256 read ordering, sample reconciliation MUST discover an unordered read-pair overlap only from shared mapped reference coordinates. A pair with no shared coordinate MUST produce no overlap edge, and lack of an overlapping or canonical F/R partner MUST NOT reject an otherwise valid read.
- **SRS-SAMPLE-008:** Pairwise overlap evidence MUST distinguish all shared reference positions from comparable nucleotide positions. Only coordinates where both reads have canonical A/C/G/T query bases MAY enter the nucleotide agreement denominator; unresolved symbols and deletions MUST remain outside that denominator, and indel/gap evidence MUST remain in its dedicated locus/variant evidence rather than receive fabricated nucleotide agreement.
- **SRS-SAMPLE-009:** A materialized overlap pair MUST be eligible for later consensus reconciliation exactly when its comparable canonical-base count is at least `minimum_comparable_bases` and its canonical-base agreement fraction is at least `minimum_overlap_agreement`. Failed rules MUST remain explicit as `comparable_bases_below_minimum` and/or `agreement_below_minimum` without mutating either read.
- **SRS-SAMPLE-010:** Compact sample evidence MUST expose pairwise overlap/admission evidence in addition to the existing read registry, sparse differential loci, and normalized variant support. This evidence MUST NOT itself produce a consensus sequence, sample-level variant verdict, majority vote, genotype, heteroplasmy estimate, or reference vote.
- **SRS-SAMPLE-011:** Sample evidence MUST expose a run-length encoded reference-coordinate coverage topology derived only from selected post-trim read segments. Each maximal interval MUST report total read depth plus forward/reverse orientation depth, with total depth equal to the orientation-depth sum. Coverage MUST include every independently placed read regardless of pairwise overlap eligibility and MUST NOT imply canonical-base agreement, consensus admission, or biological strand independence.
- **SRS-SAMPLE-012:** Every normalized sample variant MUST retain a factorized support topology derived from its authoritative per-read support records and selected read orientations: total observing reads, eligible observing reads, forward/reverse observing reads, and eligible forward/reverse observing reads. Each read MUST contribute at most once to one normalized variant identity. The topology MUST include ineligible observations, MUST NOT count covering reference/unresolved/competing-event reads as support for the variant, and MUST NOT be interpreted as confidence, vote weight, sample-level verdict, or biological strand independence.
- **SRS-SAMPLE-013:** Sample aggregation MUST preserve the existing basecall-independent `EvidenceProfile` for every call-backed differential-locus observation and variant-associated call by matching original call index to `LocusEvidence`, then projecting A/C/G/T weights into the selected reference orientation. A valid zero-signal locus with no profile MUST remain absent; Signal MUST NOT synthesize a called-base, uniform, or reference-derived fallback. Deletion observations MUST have no nucleotide profile. Public differential-locus projection is governed by `SRS-SAMPLE-023`; profile evidence MUST NOT change placement, read/variant eligibility, or produce consensus by itself.
- **SRS-SAMPLE-014:** Every retained differential locus MUST derive factorized support topology from its authoritative observations and selected read orientations: total reads, forward/reverse reads, and reference/alternate/unresolved/deletion reads. The orientation counts and state counts MUST each sum to total reads. The topology MUST NOT treat missing coverage as reference support and MUST NOT imply weighting, confidence, consensus, or biological strand independence.
- **SRS-SAMPLE-015:** Sample aggregation MUST preserve whether each call-backed differential-locus observation and each variant-associated call falls inside an existing merged candidate-noisy call region from `signal.windowed_snr/v1`. Deletions MUST preserve this call-specific context as absent rather than fabricated. Sample reconciliation MUST NOT recompute SNR or use noisy-region membership as read admission, local contribution eligibility, variant filtering, consensus weight, confidence, or artifact classification.
- **SRS-SAMPLE-016:** Every call-backed sample observation MUST resolve one authoritative `LocusEvidence` by matching original call index and preserve its baseline-corrected A/C/G/T amplitudes, per-channel SNR, optional `EvidenceProfile`, and merged noisy-region membership in one reference-oriented `CallSignalEvidence`. Reverse reads MUST reorder all A/C/G/T channel-valued evidence consistently. Deletions MUST have no fabricated call signal evidence. These internal quantitative observations MUST NOT by themselves define contributor eligibility, consensus weight, calibrated confidence, or artifact classification.
- **SRS-SAMPLE-017:** Every retained differential locus MUST expose the number of observations with an available basecall-independent nucleotide profile and factor that count by selected forward/reverse orientation. Profile-available count MUST equal the sum of profile-forward/profile-reverse counts and MUST NOT exceed non-deletion read count. Unresolved calls MAY count when a valid profile exists; zero-signal missing profiles and deletions MUST NOT. Profile availability MUST NOT be interpreted as contributor eligibility, agreement, weight, confidence, or a consensus vote.
- **SRS-SAMPLE-018:** Every retained differential-locus observation MUST have an explicit structural nucleotide-contribution state. A call-backed observation with a present basecall-independent profile MUST be `Eligible`, including unresolved calls and calls inside candidate-noisy regions. A call-backed observation without a profile MUST be `MissingProfile`; a deletion MUST be `DeletionEvent` and remain on the separate gap/event path. This first contributor policy MUST NOT use relative quality, SNR, noisy-region membership, variant eligibility, or any new threshold as a rejection or weighting rule.
- **SRS-SAMPLE-019:** Every retained differential locus MUST accumulate reference-oriented A/C/G/T `EvidenceProfile` vectors from `Eligible` observations using unit read mass, retaining total plus forward-only and reverse-only support vectors and contributor counts. Total contributors MUST equal forward plus reverse contributors, and total support MUST be the channel-wise sum of the orientation partitions. Missing-profile observations and deletion events MUST contribute no nucleotide mass. This accumulator MUST NOT use amplitude, SNR, relative quality, noisy-region state, or alignment score as a weight and MUST NOT normalize or select a consensus base.
- **SRS-SAMPLE-020:** Each non-empty total/forward/reverse unit-mass nucleotide-support partition MUST derive an arithmetic mean A/C/G/T evidence profile by dividing channel support by that partition's contributor count; empty partitions MUST have no mean profile. Mean profiles MUST preserve contributor counts and MUST NOT select a nucleotide, classify agreement/discordance, assign confidence, or introduce quality/SNR/artifact weighting.
- **SRS-SAMPLE-021:** Every non-empty total/forward/reverse eligible-profile partition MUST retain threshold-free heterogeneity geometry: mean within-profile Gini impurity, between-profile dispersion, and total mean-profile heterogeneity, with total heterogeneity equal to within impurity plus between dispersion within numerical tolerance. These metrics MUST NOT be interpreted as biological mixture, heteroplasmy, confidence, or a categorical consensus state.
- **SRS-SAMPLE-022:** When both forward and reverse mean nucleotide profiles exist, Signal MUST retain their Total Variation distance in [0, 1]; otherwise the directional distance MUST be absent. This distance MUST remain a descriptive orientation-profile metric and MUST NOT introduce a discordance threshold, strand-bias verdict, confidence value, or consensus decision.
- **SRS-SAMPLE-023:** Public sample differential-locus evidence MUST project the existing factorized support topology and, for each call-backed observation, the existing candidate-noisy-region membership plus optional normalized reference-oriented A/C/G/T `EvidenceProfile`. A call-backed observation with no real profile MUST omit `profile`; a deletion MUST omit both `profile` and `in_noisy_region`. The report layer MUST NOT recompute signal evidence, fabricate missing profiles, expose corrected amplitudes/SNR, or interpret topology/profile/noise as a consensus, confidence value, artifact label, or biological independence claim.

## 8. Primary-sequence differences

- **SRS-VAR-001:** Variants MUST derive only from the selected primary-sequence alignment.
- **SRS-VAR-002:** Canonical primary A/C/G/T mismatches MAY produce normalized SNV observations; unresolved primary N differences MUST be excluded and counted. A mixed call may still produce a primary-sequence difference from its strongest channel, but mixed supporting signal MUST remain explicit and MUST NOT be presented as an ordinary clean SNV.
- **SRS-VAR-003:** Contiguous gaps MAY produce insertions/deletions no longer than configured changed length, excluding the anchor.
- **SRS-VAR-004:** Indel representation MUST preserve the canonical gap placement selected by alignment under `SRS-ALN-012`; variant construction MUST NOT independently left-shift, rotate, or otherwise move a repeat-equivalent indel. When no aligned left flank exists, the actual reference predecessor MUST be derived when possible; a true leading linear insertion/deletion MUST use a real right-anchor representation. Circular origin-spanning representation MUST preserve the alignment-selected side of the rCRS seam.
- **SRS-VAR-005:** Reported variants MUST be normalized and include only 1-based `position`, reference/alternate alleles, kind, and a direct `calls` array. Contig, classification, and normalization labels MUST NOT be duplicated in compact JSON.
- **SRS-VAR-006:** Every public variant-associated call MUST preserve its role, reference-oriented called `base`, co-located reference-oriented A/C/G/T primary-event channel heights, and uncalibrated `quality`. Original call index, ABIF PLOC, mapped call position, trace-strand primary/ambiguity, peak positions/sources, and vendor evidence MUST remain internal. Deletions MUST contain real flanks only and MUST NOT fabricate deleted-base signal/quality.
- **SRS-VAR-007:** Variant construction MUST preserve observed call/reference mappings and the alignment-selected canonical indel topology. Reported allele anchoring MAY add the required adjacent reference base but MUST NOT relocate the event through a repeat or across the rCRS origin seam.
- **SRS-VAR-008:** Variant alleles MUST use the reference strand. Public call bases and A/C/G/T peak labels MUST be projected to the reference strand, including reverse alignments. Only the four co-located primary-event heights are emitted; selected peak positions/sources and broader per-call peak objects remain internal.
- **SRS-VAR-009:** Genotype, zygosity, homoplasmy, heteroplasmy fraction, phase, PHFinder, pathogenicity, and clinical significance are prohibited in Signal output.
- **SRS-VAR-010:** Every reported variant's normalized 1-based anchor position MUST lie in at least one configured inclusive biological region.
- **SRS-VAR-011:** Every SNV supporting call and every inserted-base supporting call MUST meet the configured maximum-channel peak floor and strictly exceed the configured relative-quality threshold. Insertion flanks and deletion flanks MUST NOT be used for this supporting-evidence gate.
- **SRS-VAR-012:** A normalized SNV whose supporting call retains more than one co-localized qualifying channel under the active basecalling method MUST remain in observed/sample evidence but MUST be ineligible for clean SNV reporting with exclusion reason `mixed_supporting_signal`. This rule MUST NOT be generalized to insertions/deletions without a separately specified mixed-length method.

## 9. JSON output

- **SRS-OUT-001:** A successful core CLI invocation MUST create exactly one command-specific result: analysis at `results/<trace-stem>.json`, reference-free basecalls at `results/<trace-stem>.basecalls.json`, or sample evidence at `results/<sample-id>.sample.json`. VCF/BCF, legacy JSON, duplicate compatibility output, and output-path/format compatibility options MUST NOT be created. Operational logging MUST remain a separate append-only sidecar.
- **SRS-OUT-002:** Analysis JSON MUST validate against Draft 2020-12 `docs/schemas/analysis-v7.schema.json` and identify `signal.analysis/v7`. Basecall JSON MUST validate against `docs/schemas/basecalls-v2.schema.json` and identify `signal.basecalls/v2`. Sample evidence JSON MUST validate against `docs/schemas/sample-evidence-v8.schema.json` and identify `signal.sample_evidence/v8`. Strict configuration MUST remain schema version 5.
- **SRS-OUT-003:** Analysis and basecall JSON MUST include compact input/configuration identities, read call count and trim interval, merged candidate-noisy regions, and warning counts. Analysis MUST additionally include reference identity, selected alignment summary, and normalized variants with concise mapped calls; basecall MUST include primary, ambiguity, and retained sequences. Sample evidence JSON MUST include the sample identifier, shared reference/configuration identities, a deterministic read registry with reviewer-facing filenames plus SHA-256 identities and concise selected-alignment summaries, a deterministic pairwise overlap/admission graph, sparse differential loci with factorized support topology plus per-read state/base/quality and optional normalized A/C/G/T profile/noisy context, and normalized variant support with named reads plus peak/quality evidence while omitting consensus and interpretation. Software/build provenance MUST remain absent until a stable versioning strategy is defined.
- **SRS-OUT-004:** Analysis JSON MUST omit trace filenames, full primary/ambiguity/retained sequences, individual rolling windows, gapped alignment rows, operation runs, alignment score and redundant metrics, method constants, selected-peak positions/sources, call indexes/PLOC coordinates, penalties/calibration flags, vendor data, expanded configuration, variant contig/classification/normalization labels, and redundant warning fields. Basecall JSON MUST include full primary/ambiguity/retained sequences but omit reference, alignment, variants, rolling windows, per-call tables, peaks, penalties/calibration flags, and vendor data.
- **SRS-OUT-005:** JSON MUST use concise context-defined coordinate names: biological variant/locus `position` is 1-based; trim, reference-segment, and noisy-region `start`/`end` are 0-based half-open. Basecall primary and ambiguity lengths MUST equal `call_count`, and retained MUST equal the primary slice selected by trim.
- **SRS-OUT-006:** Core CLI output MUST be fully serialized, flushed, synchronized, and atomically published without overwriting. A failed core operation MUST leave no JSON result and MUST NOT replace an existing target.
- **SRS-OUT-007:** Deterministic output MUST omit timestamp, duration, host, random ID, local input/config paths, unordered maps, and all compatibility aliases.
- **SRS-OUT-008:** Rust MUST append timestamped, run-correlated INFO/WARN/ERROR stage records without writing operational records to application stdout/stderr. Standalone single-read operations MUST use `$SIGNAL_LOG_DIR/<trace-stem>.log`; one sample operation MUST use exactly one `$SIGNAL_LOG_DIR/<sample-id>.log` (default root `logs/`) and MUST route every nested trace-processing stage through that same sample logger rather than opening per-trace files. Records MUST cover each processing-stage boundary with concise aggregate metrics and timings, MUST remain one physical line, and MUST omit complete sequences, peak arrays, configured region contents, alleles, operation strings, and JSON bodies.
- **SRS-OUT-009:** Each excluded variant candidate MUST produce a WARN record containing its kind, contig, normalized position when available, and every rejection reason. Removed-variant records MUST omit reference and alternate alleles.

## 10. External clean batch orchestration

- **SRS-BAT-001:** The external batch runner MUST validate the selected manifest prefix, trace directory, reference, configuration, selected trace workload, identities, destinations, and cleanup targets before deleting any artifact.
- **SRS-BAT-002:** Batch preflight MUST reject invalid or duplicate selected IDs, missing selected traces, traces matching multiple selected samples, unsafe target types, path escapes, and symlinked traces or cleanup targets.
- **SRS-BAT-003:** Unless `--no-build` is selected, the release build MUST complete successfully before cleanup. In all modes the selected binary MUST be a regular file before cleanup.
- **SRS-BAT-004:** Cleanup MUST destructively remove only selected `results/<sample-id>/` directories and selected `$SIGNAL_LOG_DIR/<sample-id>.log` files. It MUST preserve every unselected result directory and unrelated or legacy trace log.
- **SRS-BAT-005:** After cleanup, each selected trace MUST run through an isolated one-file core CLI invocation and each generated JSON MUST be placed without overwrite. Those helper invocations MUST write operational logs only inside their temporary workspaces. When every trace for a selected sample succeeds, the wrapper MUST run the sample command over the complete trace set, persist only `$SIGNAL_LOG_DIR/<sample-id>.log`, and atomically publish the aggregate as `results/<sample-id>/<sample-id>.json`. The sample log MUST contain the nested trace-stage records produced by the authoritative read pipeline. The wrapper MUST NOT weaken the core CLI's no-overwrite semantics.
- **SRS-BAT-006:** If any trace for a sample fails, the batch runner MUST NOT publish that sample's aggregate JSON. Batch cleanup is not an all-workload transaction: a failure after cleanup MAY leave successful new results/logs from earlier traces or samples and MUST NOT claim to restore the removed prior selected artifacts; the final status MUST report trace and sample failures.

## 11. Compatibility, quality, and validation

- **SRS-COMPAT-001:** Apollo comparisons MUST follow `compatibility.md`; known defects are intentional divergences, not parity failures.
- **SRS-COMPAT-002:** Approved differential evidence MUST compare exact decoded arrays and unaffected deterministic results; normalized variants compare by full tuple without ignoring extras/missing calls.
- **SRS-NFR-001:** Production code MUST forbid unsafe Rust, deny deprecated API use at crate roots, and avoid production `unwrap`/`expect`. First-party source MUST NOT suppress the deprecated-use diagnostic.
- **SRS-NFR-002:** Scientific stage functions MUST be side-effect-free and return typed results; only pipeline-level operational logging and report publication write files.
- **SRS-NFR-003:** Representative 500–1,000 base release analysis SHOULD complete within 30 seconds and 512 MiB on a documented host.
- **SRS-NFR-004:** Every Rust source MUST have an exact current `docs/src` counterpart.
- **SRS-NFR-005:** First-party production Rust MUST NOT retain deprecated compatibility APIs, explicit legacy/backward-compatibility feature paths, or suppress `deprecated`, dead-code, unreachable-code, or unused-code diagnostics to preserve obsolete source. The required Rust source-policy validator MUST fail such scaffolding.
- **SRS-VAL-001:** Parser, calling, signal processing, QC, alignment, normalization, JSON, and atomic publication MUST have focused boundary/adversarial tests.
- **SRS-VAL-002:** A synthetic canonical ABIF MUST exercise end-to-end forward/reverse and variant behavior without identifying data.
- **SRS-VAL-003:** Real-trace release evidence MUST follow `data.md`; ignored local data MUST never be a build/test prerequisite.
- **SRS-VAL-004:** Rust source-policy validation, format, check, Clippy warnings-denied, all tests, rustdoc, schema/example validation, TOML validation, docs mirror, and rCRS identity gates MUST pass.
- **SRS-VAL-005:** Local threshold research MUST use a dedicated validation measurement path that reuses the authoritative read/sample science implementation, emits every covered reference locus including clean reference matches, publishes deterministic no-overwrite `signal.validation_locus/v2` JSONL under ignored `validation-results/`, and does not modify production result schemas or apply candidate thresholds.
- **SRS-VAL-006:** No profile-geometry threshold may be promoted to production interpretation until its corpus, truth provenance, grouped development/holdout split, false-positive objective, threshold-selection rule, holdout performance, repeatability/reproducibility, artifact challenges, and operating-domain limitations are documented.
- **SRS-VAL-007:** Validation locus measurements MUST retain one diagnostic record per read observation with original call identity, PLOC/window coordinates, primary-peak placement, signal-evidence event placement, channel-peak evidence, reference-oriented corrected amplitude/SNR/profile values, selected orientation, and noisy-region membership. Deletions MUST retain null call/event diagnostics. Validation diagnostics MUST NOT alter event selection, basecalling, alignment, eligibility, thresholds, or production result schemas.
- **SRS-VAL-008:** Local validation-corpus orchestration MUST keep truth and grouping metadata outside the Rust scientific core; hash-bind every AB1 to exactly one validation case; reject inconsistent case metadata, duplicate trace ownership, and source groups assigned to multiple development/holdout groups; run the authoritative `signal-validation` path independently for each case; validate measurement identity and read provenance; and publish selected corpus artifacts only after all selected cases succeed. Existing corpus outputs MUST NOT be overwritten, local trace paths MUST NOT enter the corpus index, and `declared_direction` MUST remain validation metadata rather than placement input.
- **SRS-VAL-009:** Local validation research dataset preparation MUST consume only a completed `signal.validation_corpus/v1`, revalidate corpus/measurement/read identity, join case/truth/holdout and read acquisition metadata to deterministic locus and observation tables without local AB1 paths, hash-bind the source corpus and generated tables, and publish without overwrite. Joined rows MUST be processed case-by-case rather than retained as one in-memory dataset. Descriptive geometry summaries MAY report exact empirical distribution statistics but MUST NOT choose a candidate threshold, optimize a classifier, tune against holdout data, infer biological truth, or modify production result schemas.
- **SRS-VAL-010:** Local validation audit MUST consume one hash-matched completed corpus and research dataset plus the corpus validation logs; bind parsed deterministic read-stage metrics back to corpus reads by SHA-256; derive only observational review strata; and publish deterministic no-overwrite `signal.validation_audit/v1` tables/index without local AB1 paths, timestamps, run IDs, or raw log text. Relative read outlier rules MUST be conditioned on amplicon and declared direction, MUST NOT apply when fewer than 20 reads exist in a stratum, and MUST record their empirical boundaries. Mixed reference/alternate loci MAY be flagged with retained-read edge context. Audit flags MUST NOT alter truth metadata, threshold-fit inclusion, holdout assignment, read/sample eligibility, production configuration, or production outputs.
- **SRS-VAL-011:** Local validation curation-queue preparation MUST consume only one completed `signal.validation_audit/v1`; verify audit table hashes and column contracts; include every mixed locus and every read with non-empty audit flags exactly once; preserve stable review-item identifiers plus audit-supported evidence; and publish deterministic no-overwrite `signal.validation_curation_queue/v1` evidence separately from an editable blank decisions template. Queue generation MUST NOT assign biological truth, exclusion, development/holdout membership, threshold-fit inclusion, reviewer decisions, or production behavior. Generated evidence ordering MAY group recurrent loci for review but MUST NOT expose a numeric biological priority score.
- **SRS-VAL-012:** Local poly-C phase-instability research MUST consume a completed validation corpus through the validated measurement loader; identify only rCRS HV2 positions 303–315 (`CCCCCCCTCCCCC`, T310 interrupt) and HV1 positions 16184–16193 (`CCCCCTCCCC`, T16189 interrupt) after verifying the represented tract sequence; include only reads with a validation observation at every reference position of the complete tract; and publish deterministic no-overwrite `signal.validation_polyc_phase/v1` observation/summary tables. For call-backed observations, before/after path region MUST derive from call order relative to the complete tract call span, not from global genomic ordering; signed reference distance and previous/next reference neighbors MUST follow circular rCRS sequencing order across the 16569/1 seam. An outside-tract observation without a call index MUST remain unresolved rather than receiving an inferred side. The artifact MAY report raw profile impurity and previous/current/next reference-base mass plus read-local interrupt evidence, but MUST NOT infer tract genotype, biological length heteroplasmy, artifact truth, a phase score, recovery cutoff, confidence attenuation, or production behavior.
- **SRS-VAL-013:** Local post-poly-C phase-hypothesis research MUST consume one hash-matched completed `signal.validation_polyc_phase/v1` artifact and publish deterministic no-overwrite `signal.validation_phase_hypotheses/v1` window and candidate tables. Each non-zero integer reference offset MUST be evaluated in selected sequencing order across consecutive profile-bearing post-tract observations; only positions where the zero-phase and shifted reference bases differ may contribute to that candidate. Candidate evidence MUST preserve informative-position count plus mean profile mass on the zero-phase base, shifted base, and residual bases, and MUST retain the complete tested offset curve. Research window size, stride, and offset range MUST be explicit artifact method parameters. The analysis MUST NOT rewrite observed evidence, select a winning phase, infer an indel/genotype/length heteroplasmy, define a recovery cutoff, confidence weight, no-call rule, or production behavior.
- **SRS-VAL-014:** Local post-poly-C phase-characterization research MUST consume one hash-matched completed `signal.validation_phase_hypotheses/v1` artifact and publish deterministic no-overwrite `signal.validation_phase_characterization/v1` persistence, trajectory, and strata tables. Persistence MUST compare consecutive source windows for one read/tract at each unchanged candidate offset without selecting a winner. Trajectories MUST retain exact source-window start distance in sequencing order rather than defining recovery bins, and strata MAY group by tract, amplicon, orientation, and observed interrupt base. The analysis MUST retain every declared candidate offset, MUST NOT interpret the interrupt base as genotype, and MUST NOT define a phase-state classifier, breakpoint, recovery threshold, confidence weight, indel/length-heteroplasmy call, no-call rule, or production behavior.
- **SRS-VAL-015:** Local poly-C opposite-orientation control research MUST consume one completed `signal.validation_corpus/v1` through the validated research loader and publish deterministic no-overwrite `signal.validation_polyc_orientation_controls/v2` observation and matched-locus tables. A control group MUST be bounded by validation case, tract, reference locus, and post-tract selected orientation; both post and opposite-orientation control reads MUST have complete tract coverage so after/before state is proven from the shared call-order geometry. Selected alignment orientation, never declared direction, MUST determine role. Every eligible read MUST be retained without best-pair selection or Cartesian pair expansion. Read-local aligned interrupt evidence MUST be preserved per observation, and locus summaries MAY expose only deterministic sorted unique post/control interrupt-base signatures rather than a genotype or consensus. Read acquisition/PCR/amplicon metadata MAY be preserved for stratification, and arithmetic mean role profiles plus their descriptive Total Variation MAY be reported. The analysis MUST NOT infer genotype, length heteroplasmy, artifact truth, phase state, recovery threshold, confidence attenuation, evidence weighting, no-call policy, or production behavior.
- **SRS-VAL-016:** Local post-poly-C parameter-sensitivity research MUST consume one hash-matched completed `signal.validation_polyc_phase/v1` artifact and publish deterministic no-overwrite `signal.validation_phase_sensitivity/v1` parameter-set and candidate-strata tables. Every parameter set MUST reuse the authoritative `signal.validation_phase_hypotheses/v1` window/candidate implementation and the candidate-wise aggregation used by `signal.validation_phase_characterization/v1`; the grid dimensions and complete non-zero candidate offsets MUST be explicit. The analysis MUST retain every candidate independently and MUST NOT select or rank a preferred parameter set or offset, interpolate unevaluated offsets, tune against holdout truth, infer an indel/genotype/length heteroplasmy, define a phase classifier/recovery threshold/confidence weight, or modify production behavior.
