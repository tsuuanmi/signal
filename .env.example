# Copy to .env for local shell tooling. The Signal binary does not parse .env.
# Export these values into the process environment before running Signal.
SIGNAL_CONFIG=config/signal.toml
SIGNAL_LOG_DIR=logs
