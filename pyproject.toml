[project]
name = "signal"
version = "0.1.0"
description = "Deterministic analysis of a Sanger ABIF/AB1 chromatogram against a reference FASTA."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "basedpyright",
  "jsonschema",
  "ruff",
]

[tool.uv]
package = false

[tool.basedpyright]
typeCheckingMode = "basic"
